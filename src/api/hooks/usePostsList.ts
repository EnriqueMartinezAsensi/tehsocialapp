import { useEffect } from "react";

const usePostsList = (filter: string, postsPerPage: number, page: number) => {
  // Recoger datos
  // filtrar datos
  // return datos

  useEffect(() => {
    // Recogemos datos
  }, [postsPerPage, page]);
};

export default usePostsList;
